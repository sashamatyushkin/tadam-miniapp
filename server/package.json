{
  "name": "tadam-server",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "engines": { "node": ">=22.5" },
  "scripts": {
    "start": "node --env-file=.env index.js",
    "test": "node --env-file=.env test-wheel.js"
  }
}
